# tpurm: TPU Resource Manager

To use:

- Run `tpurm start` in the git directory containing the code you want to run. This directory will be your `REPO_ROOT` by default, although you could set it to something else via the env variable `TPURM_REPO_ROOT`.
- There are some env variables currently in my own `.env` that are not hardcoded.
